[project]
name = "kerrokantasi"
requires-python = ">=3.12, <3.13"
version = "2.10.7"
dependencies = [
    "django~=5.2",
    "djangorestframework",
    "django-environ",
    "django-extensions",
    "django-helsinki-health-endpoints",
    "django-munigeo",
    "django-reversion",
    "django-cors-headers",
    "django-filter",
    "pillow",
    "psycopg[c]",
    "easy-thumbnails",
    "drf-nested-routers",
    "xlsxwriter",
    "django-helusers @ https://github.com/City-of-Helsinki/django-helusers/archive/refs/tags/django-helusers-v1.2.0.zip",
    "pyjwt[crypto]",
    "jsonfield",
    "django-nested-admin",
    "django-geojson",
    "django-leaflet",
    "django-autoslug",
    "django-sendfile2",
    "django-parler<2.4",
    "langdetect",
    "sentry-sdk",
    "whitenoise",
    # Pin below 6.0.0: 6.x makes social login views POST-only, breaking the
    # admin Keycloak login GET link (405). See CHANGELOG for social-app-django 6.0.0.
    "social-auth-app-django>=5.9,<6",
    "python-pptx",
    "helsinki-profile-gdpr-api",
    "django-helsinki-notification[rest_framework] @ https://github.com/City-of-Helsinki/django-helsinki-notification/archive/refs/tags/v0.4.2.zip",
    "django-logger-extra",
    "django-resilient-logger",
    "drf-spectacular",
]

[dependency-groups]
prod = [
    "uwsgi",
    "uwsgitop",
]
dev = [
    "coveralls",
    "distro",
    "django-debug-toolbar",
    "factory-boy",
    "freezegun",
    "pytest",
    "pytest-cov",
    "pytest-django",
    "pytest-factoryboy",
    "ipython",
    "requests-mock",
]

[tool.uv]
package = false
exclude-newer = "P3D"

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = [
    # Pyflakes
    "F",
    # pycodestyle
    "E",
    "W",
    # isort
    "I",
    # pep8-naming
    "N",
    # flake8-bugbear without opinionated rules
    "B0",
    # flake8-pie
    "PIE",
    # flake8-print
    "T20",
]
extend-per-file-ignores = { "*/migrations/*" = ["E501"], "*/tests/*" = ["E501", "N802"] }

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "kerrokantasi.settings.test_settings"
norecursedirs = ["bower_components", "node_modules", ".git", "venv"]
